# Devin ACP 可扩展能力盘点（2026-08-23）

结论：Devin CLI 通过 ACP 已经暴露了比当前 Desktop 消费面更宽的能力面；当前最值得补齐的是结构化 elicitation、可编辑审批与 command revision、`chains`/`/btw`、原生会话元数据、多根工作区和规则 mention。ACP 的宿主文件/终端接口虽存在，但本项目明确禁止 Desktop 成为第二执行器，因此不应启用。【apps/desktop/src/main/acp-transport.ts:L191-L206】【apps/desktop/src/main/devin-acp-host.ts:L637-L650】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6264-L6334】

## 1. 口径与证据

- `docs/devin-cli/` 是由 `docs.devin.ai/llms.txt` 同步的官方文档镜像，应优先于二手资料。【docs/devin-cli.md:L1-L6】
- ACP SDK 是仓库依赖的 `@agentclientprotocol/sdk` 1.4.0；其包元数据指向 Agent Client Protocol 官方 TypeScript SDK，稳定入口与 experimental v2 入口分开。【apps/desktop/package.json:L33-L35】【apps/desktop/node_modules/@agentclientprotocol/sdk/package.json:L1-L16】【apps/desktop/node_modules/@agentclientprotocol/sdk/package.json:L28-L60】
- 本报告把“已声明支持”定义为官方文档、ACP schema 或 Devin 运行时 initialize/session 响应中出现的能力；把“当前已实现”定义为本仓库 Desktop 主进程、IPC 或 renderer 已有可执行路径；其余标为“未利用/部分利用”。测试 fixture 不是已部署 CLI 的证据。【apps/desktop/src/main/devin-acp-host.test.ts:L6-L16】

## 2. ACP / Devin CLI 已声明的能力

### 2.1 ACP 协议面

| 能力 | 声明内容 | 可扩展价值 |
| --- | --- | --- |
| 初始化与协商 | Agent 在 initialize 中可声明 `loadSession`、图像/音频/embedded context、MCP、session lifecycle、auth；`providers`、`nes`、`positionEncoding` 和 `_meta` 是可选扩展，其中 providers/NES/position encoding 标为 unstable。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2601-L2700】 | 让 UI 只显示当前 Devin/模型/账户真实可用的入口；vendor 能力通过 `_meta` 增量演进。 |
| Prompt 内容 | baseline 支持 text/resource link；显式能力可开启 image、audio、embedded context。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2703-L2730】 | 图片已可用，音频和更丰富的上下文附件可复用同一 prompt 通道。 |
| 会话生命周期 | `session/new/prompt/cancel/update` 是 baseline；可选 `list`、`delete`、`additionalDirectories`、`resume`、`close`，`fork` 当前为 unstable。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2763-L2845】 | 支持多任务切换、恢复、分叉、关闭和多根工作区，而不在 Desktop 自己保存第二份会话状态。 |
| 事件与可观察性 | `session/update` 可传消息、thought、tool call、plan、available commands、mode、config、session info、usage；usage 有 token 维度且标为 unstable，compaction 更新也标为 unstable并要求客户端先声明能力。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L4977-L5224】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L4370-L4421】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L5786-L5822】 | 做上下文窗口、费用、压缩过程和更完整的运行审计；在不稳定字段上应采用降级显示。 |
| Agent→Client 请求 | schema 定义了 `fs/read_text_file`、`fs/write_text_file`、`session/request_permission`、`terminal/create/output/wait_for_exit/kill/release`、`elicitation/create`；还允许任意 extension request/notification。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L293-L405】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L1183-L1383】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L1385-L1400】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2282-L2284】 | 本项目只应采用结构化用户输入和经过显式白名单的 vendor UI 请求；文件与终端执行继续由 Devin CLI 自己负责。 |
| Client 能力协商 | Client 可在 initialize 声明 fs、terminal、session compaction/configOptions、plan、auth、elicitation 等；elicitation 可分别声明 form 和 URL。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6264-L6362】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6388-L6450】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6494-L6529】 | 能力缺失时 Devin 应自动隐藏对应请求；能力存在时才打开权限明确的 UI。 |
| ACP MCP/实验扩展 | ACP schema 有 HTTP/SSE MCP，MCP-over-ACP、provider 配置、NES、plan/compaction 等实验能力；实验字段可以变化或移除。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2625-L2670】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2733-L2760】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6299-L6345】 | 适合作为实验开关或透传层，不应在未广告时静态调用。 |

### 2.2 Devin CLI / ACP 实现面

| 能力 | 官方声明 | 对 Devin Agent 的意义 |
| --- | --- | --- |
| ACP server 与动态命令 | `devin acp` 以 stdio JSON-RPC 运行；命令通过 ACP 动态广告，包含描述、参数提示和分类，并按 host/account 对登录和 workspace 命令做门控。【docs/devin-cli/reference/commands.md:L297-L331】 | Desktop 可以直接复用 CLI 的命令、技能和权限，而不是复制命令表。 |
| 会话及命令扩展 | CLI 文档列出 `/ask`、`/plan`、`/compact`、`/context`、`/fast`、`/loop`、`/btw`、`/session-stats`、`/workspace`、`/add-dir`、`/remove-dir`、`/mcp` 等 ACP 命令；较新的 `/recap`、`/rename` 也已在 stable 版本中出现。【docs/devin-cli/reference/commands.md:L318-L331】【docs/devin-cli/changelog/stable.md:L9-L19】【docs/devin-cli/changelog/stable.md:L123-L140】 | 新命令只需被广告并在命令面板执行；`/btw` 还能提供并行 side-chat，`/rename`、`/share` 可减少本地索引与云端状态分叉。 |
| Rules / Skills | Rules 在每个 session 注入持久约束；Skills 是可由用户或 agent 触发、可限制工具/权限、可切换模型并可作为 subagent 执行的复用单元。【docs/devin-cli/extensibility/rules.md:L5-L15】【docs/devin-cli/extensibility/skills/overview.md:L5-L10】【docs/devin-cli/extensibility/skills/overview.md:L23-L47】【docs/devin-cli/extensibility/skills/overview.md:L81-L110】 | 项目规范、审查流程、部署流程和团队知识可随仓库配置交付，Desktop 不需执行 skill 内容。 |
| MCP | MCP server 工具像内置工具一样被发现和调用，并受同一 permission system 保护；MCP prompts 会成为 `/mcp__<server>__<prompt>`，且同样通过 ACP 广告。【docs/devin-cli/extensibility/mcp/overview.md:L5-L11】【docs/devin-cli/extensibility/mcp/overview.md:L60-L79】【docs/devin-cli/extensibility/mcp/overview.md:L83-L111】 | 可接 issue tracker、数据库和内部 API；执行与 OAuth 仍归 Devin CLI。 |
| Hooks | 生命周期 hook 可在工具前后、权限请求、prompt、停止、压缩和 session 起止时运行；输出可批准/阻断、注入上下文或改写工具输入。【docs/devin-cli/extensibility/hooks/overview.md:L5-L11】【docs/devin-cli/extensibility/hooks/overview.md:L61-L76】【docs/devin-cli/extensibility/hooks/overview.md:L138-L181】 | 可把组织安全策略、审计和外部通知接入每个 Devin session。 |
| Custom subagents | Subagents 默认启用，有只读探索和通用两种内置 profile；自定义 profile 可声明 system prompt、工具限制和模型覆盖，但仍是实验能力。【docs/devin-cli/subagents.md:L92-L124】【docs/devin-cli/subagents.md:L216-L229】 | 可把审查、研究、迁移等工作拆给专用 worker；Desktop 只展示其事件，不再实现第二执行器。 |
| Plugins | Plugin 可捆绑 skills、rules、hooks、MCP 和 custom subagents；目前 closed beta，且不同 surface 有限制（plugin subagent 仅 CLI/Desktop，prompt hook 仅 CLI/local）。【docs/devin-cli/extensibility/plugins/overview.md:L5-L19】【docs/devin-cli/extensibility/plugins/overview.md:L49-L74】 | 适合团队分发能力包，但必须显示 beta/权限/来源，并接受 surface 差异。 |
| Handoff / Sandbox / Permissions | `/handoff` 把上下文、分支和未提交改动交给云端 VM，可用于浏览器、CI、长任务和并行执行；sandbox 失败时 fail-closed，Windows/ACP 内嵌也不支持 OS sandbox；权限模式由 CLI 管理。【docs/devin-cli/handoff.md:L5-L37】【docs/devin-cli/sandbox.md:L9-L27】【docs/devin-cli/reference/permissions.md:L13-L37】 | 可将超出本机能力的任务转云端，同时保持本地安全边界；Desktop 不应绕过 CLI 的 sandbox/permission。 |

## 3. 仓库当前实现与缺口

| 能力 | 当前实现 | 状态与证据 |
| --- | --- | --- |
| 初始化、认证、基础 session | Host 发送 protocol v1 initialize，保留 runtime capabilities；使用广告的 auth method 调 `authenticate`，支持 new/load/list/delete/additionalDirectories，并对能力和锁定 session 做门控。【apps/desktop/src/main/devin-acp-host.ts:L202-L243】【apps/desktop/src/main/devin-acp-host.ts:L246-L272】【apps/desktop/src/main/devin-acp-host.ts:L274-L366】【apps/desktop/src/main/devin-acp-host.ts:L683-L691】 | **已实现**。这是目前最完整的 ACP 面。 |
| Prompt、图片、资源 mention | Host 支持 text/image/resource/resource_link；renderer 已根据 prompt capability 与模型能力门控图片，mention serializer 会把小文本嵌入或发送 URI link。【apps/desktop/src/shared/acp-types.ts:L162-L202】【apps/desktop/src/renderer/lib/capabilities.ts:L33-L70】【apps/desktop/src/main/mention-prompt.ts:L25-L64】 | **已实现** image/embedded/resource；**未实现** audio（当前显式 content union 没有 audio，renderer 也只有 text/image）。【apps/desktop/src/shared/acp-types.ts:L162-L197】【apps/desktop/src/renderer/lib/capabilities.ts:L38-L40】 |
| Stream/UI 事件 | Transport 注册 `session/request_permission` 与 `session/update`；normalizer 已处理 message/thought/tool/plan/commands/mode/config/session_info/usage，renderer 应用除 usage 外的主要更新。【apps/desktop/src/main/acp-transport.ts:L191-L206】【apps/desktop/src/renderer/lib/acp-normalizer.ts:L40-L167】【apps/desktop/src/renderer/App.tsx:L1065-L1110】 | **部分实现**。usage 能解析但没有进入 UI 状态；compaction、plan_update、plan_removed、elicitation_complete 等不在当前 reducer 词汇中。 |
| Dynamic model/mode/config/commands | Snapshot 从 initialize/session 结果构建模型、mode、config 和命令；命令面板执行被广告的 slash command，handoff 也先检查命令是否存在。【apps/desktop/src/main/runtime-adapter.ts:L51-L114】【apps/desktop/src/main/index.ts:L269-L333】【apps/desktop/src/renderer/App.tsx:L1840-L1855】 | **已实现**基本动态能力；`AvailableCommand` 的 input hint/category/icon 等 metadata 只部分保留/展示，需确认再扩展 schema。【apps/desktop/src/shared/acp-types.ts:L234-L239】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L5613-L5678】 |
| `resume` / `close` / `fork` | Host 有 resume/close 方法，但默认切换路径使用缓存或 `session/load`；runtime start 不调用 resume，IPC allowed command 没有 resume/close/fork。【apps/desktop/src/main/devin-acp-host.ts:L368-L420】【apps/desktop/src/main/index.ts:L209-L244】【apps/desktop/src/main/index.ts:L767-L767】 | **部分/未利用**。`fork` Host/IPC/UI 均缺失；ACP fork 还是 unstable，需先确认 Devin 广告和 transcript 语义。 |
| 规则/技能 | Desktop 扫描多种 skill roots、缓存 session snapshot，并把 skill/file/directory mention 序列化为 CLI 可消费的文本或资源；规则由 CLI 读取，但 Desktop 没有 ACP rule list/规则 mention 数据模型。【apps/desktop/src/main/skill-index.ts:L9-L19】【apps/desktop/src/main/skill-index.ts:L79-L98】【apps/desktop/src/main/mention-prompt.ts:L25-L64】【apps/desktop/src/shared/mentions.ts:L5-L39】 | **Skill 部分实现；规则未利用**。扩展规则选择器需要使用 Devin 的规则列表，而不是静态扫描另一个目录。 |
| 重命名与分享 | UI 重命名只写本地 session-index；没有调用 ACP `/rename` 或 vendor rename 方法。【apps/desktop/src/main/session-index.ts:L148-L157】【apps/desktop/src/main/index.ts:L634-L650】 | **部分实现**。运行时探测到的 vendor `_cognition.ai/session/rename` 可作为可选适配，但应先 capability-gate。 |
| Agent→Client elicitation / vendor UI | `handleServerRequest` 除 permission 外全部返回“未实现”；Transport 只注册两个 client handlers；initialize 默认 `clientCapabilities` 是 `{}`。【apps/desktop/src/main/devin-acp-host.ts:L637-L650】【apps/desktop/src/main/acp-transport.ts:L191-L206】【apps/desktop/src/main/devin-acp-host.ts:L212-L217】 | **未利用**。结构化提问和可编辑审批是实际缺口；fs/terminal 未广告属于本项目有意维持的执行边界。 |
| Usage / cost / compaction | `UsageState` 和 normalizer 可承接 token/context/cost，但 App 明确关闭 context card；当前无 compaction client capability 或 UI。【apps/desktop/src/shared/conversation.ts:L121-L129】【apps/desktop/src/renderer/lib/acp-normalizer.ts:L350-L365】【apps/desktop/src/renderer/App.tsx:L245-L248】 | **部分实现**，并且 ACP usage/compaction 均有 unstable 或精度风险。 |
| CLI 扩展执行边界 | MCP、skills、hooks、plugins、subagents、sandbox 全部由 Devin CLI 运行；Desktop 只发送 prompt/命令和展示事件，不实现第二 executor。【docs/devin-cli/extensibility/index.md:L39-L49】【apps/desktop/src/main/index.ts:L269-L333】 | **已遵守边界**。扩展价值主要来自 `.devin/` 配置和 ACP 动态命令；UI 只需发现、门控和呈现。 |
| 静态功能门 | checkpoint、steer、完整 tool diff、精确 cost 目前明确标为 unsupported。【apps/desktop/src/shared/capabilities.ts:L73-L87】 | **不应伪造支持**；除非 ACP/Devin 后续广告对应能力，否则保持禁用。 |

## 4. 本机实时探测（2026-08-23）

这是已认证本机安装的 Devin CLI 运行时一手结果，不等同于协议保证；结果依赖 CLI 版本、账户、组织策略和 feature flags。基础 smoke 流程会通过 `DevinAcpHost` 创建、加载并删除临时 session，代码中可复核其清理路径。【apps/desktop/scripts/live-cli-smoke.mjs:L33-L40】【apps/desktop/scripts/live-cli-smoke.mjs:L95-L118】

复现入口：

```bash
devin --version
# 以下 smoke 会通过已认证账号发送最小 prompt，并创建后删除临时 session；仅在明确接受该副作用时运行。
DEVIN_LIVE_TEST=1 pnpm --dir apps/desktop smoke:devin
```

本机版本为 `devin 3000.5.20 (2d902011)`。额外的 initialize/session 探测使用同一 `DevinAcpHost` 请求流，创建临时 session 后立即 `session/delete` 清理，观察到：

| 运行时观察 | 结果 | 交叉依据与当前含义 |
| --- | --- | --- |
| initialize | `protocolVersion=1`；`loadSession=true`；`promptCapabilities={image:true,audio:false,embeddedContext:true}`；`mcpCapabilities={http:false,sse:false}`；`sessionCapabilities={list:{},delete:{},additionalDirectories:{}}`；`authMethods=[devin-browser]`。 | 字段含义与门控规则由 ACP schema 定义。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2601-L2647】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2703-L2730】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2733-L2760】 |
| `agentCapabilities._meta` | 发现 `multiRootWorkspace`、`sessionRename`、`sessionShare`、`documentLifecycle`、`userEdits`、`terminalLifecycle`、`userConfig`、`userShellCommand`、`editableCommands`、`commandRevision`、`chains`、`megaplan`、`ruleMentions`（均为 `cognition.ai/...: true`）。 | `_meta` 是 ACP 保留的扩展空间；Host 会合并并保留未知字段，但当前没有这些字段的 typed UI。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2695-L2700】【apps/desktop/src/main/devin-acp-host.ts:L708-L726】 |
| 临时 session | `session/new` 返回 modes `accept-edits/ask/plan/bypass`；config options 动态给出 `mode`/`model`，随后已 delete。 | 当前 runtime snapshot 已能消费 session modes/config；与源码路径一致。【apps/desktop/src/main/runtime-adapter.ts:L59-L84】【apps/desktop/src/main/runtime-adapter.ts:L116-L128】 |
| `available_commands_update` | 包含 `/login`、`/logout`、`/status`、`/workspace`、`/ask`、`/plan`、`/compact`、`/context`、`/fast`、`/loop`、`/recap`、`/session-stats`、`/rename`、`/share`、`/mcp`、`/bug`、`/help` 及动态 skills；命令带 description，部分有 `input.hint`，`_meta` 带 icon/category。 | ACP `AvailableCommand` 要求 name/description，可选 input/_meta；CLI 文档也声明动态命令会带描述、提示和分类，stable changelog 记录 `/recap`、`/rename`、`/share`。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L5613-L5678】【docs/devin-cli/reference/commands.md:L318-L331】【docs/devin-cli/changelog/stable.md:L9-L19】【docs/devin-cli/changelog/stable.md:L69-L77】 |
| chains / side-chat | 默认 client `clientCapabilities:{}` 时不出现 `/btw`；只在 initialize 的 client `_meta` 广告 `{"cognition.ai/chains":true}` 后出现。 | CLI 已声明 `/btw` 是 sidechain；Desktop 默认未广告该 vendor capability，因此当前未解锁。【docs/devin-cli/reference/commands.md:L452-L457】【docs/devin-cli/changelog/stable.md:L31-L31】【apps/desktop/src/main/devin-acp-host.ts:L212-L217】 |
| rule mentions | Agent 广告 `cognition.ai/ruleMentions:true`；读取 `_cognition.ai/rules/list`（`{sessionId}`）成功，返回 `name/path/provider/providerLabel/trigger/userInvocable/scope`，实测 scope 有 global/workspace；当前 MentionKind 只有 file/directory/skill。 | Devin CLI 已提供 `devin rules list`；Desktop 类型未覆盖 rule，属于可直接利用的 vendor 缺口。【docs/devin-cli/reference/commands.md:L153-L167】【apps/desktop/src/shared/mentions.ts:L5-L39】 |
| vendor rename | `session/rename` 失败；`_cognition.ai/session/rename` 携带 `{sessionId,title}` 成功返回空对象；`name/newTitle/value` 参数均失败。空 session 未在 list 中验证持久化标题，故只记录“请求被接受”。 | stable CLI 已有 `/rename`；Desktop 当前本地重命名，适配时必须 capability-gate、保留本地回退并验证远端持久化。【docs/devin-cli/changelog/stable.md:L9-L19】【apps/desktop/src/main/session-index.ts:L148-L157】 |

## 5. 扩展优先级、依赖与风险

| 优先级 | 建议扩展 | 必要依赖 | 主要风险 / 约束 |
| --- | --- | --- | --- |
| P0 | **结构化用户输入（elicitation）**：支持 `ask_user_question` 的 form/URL 请求。 | 在 initialize 仅广告真实支持的 `elicitation.form/url`；在 SDK client 注册 `elicitation/create` 与 `elicitation/complete`；增加可取消、按 session/tool 关联的 UI。【apps/desktop/node_modules/@agentclientprotocol/sdk/dist/acp.d.ts:L530-L555】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6323-L6334】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6494-L6529】 | Devin changelog 已确认 `ask_user_question` 使用标准 ACP elicitation；URL 外跳和表单数据必须经过用户确认，未知 action 不可当作已知 action。【docs/devin-cli/changelog/stable.md:L103-L106】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L8221-L8320】 |
| P0 | **可编辑审批、command revision 与 `chains` side-chat**：完整消费 `editableCommands`、`commandRevision`、`chains`，在主任务运行时提供 `/btw` 并行问答。 | `editableCommands`/`commandRevision` 只按 Agent 运行时能力门控，不能误广告为客户端能力；只有完成 side-chat handler 后才在 client `_meta` 声明 `chains`。permission UI 保留原始 tool/option metadata，并按 session/chain 路由流。【docs/devin-cli/changelog/stable.md:L31-L31】【docs/devin-cli/changelog/stable.md:L123-L139】 | vendor wire contract 不稳定；不能仅凭 capability 名猜 payload。命令修改后必须重新展示并由用户确认，未知请求继续 fail-closed。 |
| P2 / 条件 | **Usage / context / compaction**：仅在后续运行时明确广告后展示上下文占用、token、成本和压缩摘要。 | initialize 广告 `session.compaction`（unstable）后才处理 `usage_update`、`compaction_update`、`compaction_summary_chunk`；成本字段只作“CLI 报告值”。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L5172-L5219】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L6392-L6403】 | 当前实测 initialize 未广告 compaction，代码也明确不能验证精确 cost/cache，因此不应据此做计费承诺。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L4370-L4421】【apps/desktop/src/shared/capabilities.ts:L82-L87】 |
| P1 | **多根工作区 UI**：把已实现的 `additionalDirectories` Host 参数接到新建/加载任务 UI，并展示当前根目录集合。 | 仅在 `sessionCapabilities.additionalDirectories` 和 `cognition.ai/multiRootWorkspace` 广告时启用；附加目录必须显式选择并持久显示。【apps/desktop/src/main/devin-acp-host.ts:L274-L324】【apps/desktop/src/main/runtime-adapter.ts:L116-L128】 | additionalDirectories 会扩大读写、rules、skills、hooks 和 MCP 的作用范围，不能默认静默附加。当前实测没有广告 `resume`、`close`、`fork`，这些操作只能作为未来条件能力。 |
| P1 | **规则列表与规则 mention**：增加规则选择器，序列化为 vendor `_cognition.ai/rules/list` 或规则引用。 | 先检查 `cognition.ai/ruleMentions`；扩展 `MentionKind`、IPC、UI 和 prompt serializer；显示 provider/scope/trigger/userInvocable。【docs/devin-cli/reference/commands.md:L153-L167】【apps/desktop/src/shared/mentions.ts:L5-L39】 | 规则可能包含敏感组织策略；必须按 session/workspace scope 过滤，不能把全局规则内容误发到不相关 session；vendor method 不是 ACP 稳定方法。【docs/devin-cli/extensibility/rules.md:L100-L119】 |
| P1 | **原生重命名、分享与命令表单**：同步消费 `sessionRename`/`sessionShare`；根据 command 的 `input.hint`、category、icon 生成参数输入和分组 UI。 | 对 `_cognition.ai/session/rename` 和 share 请求封装并 capability-gate；分享属于外部状态变更，执行前明确确认；保留本地标题作为断线降级而非唯一真相。 | vendor rename 的持久化尚未由空 session 探测验证；share 会把会话暴露给团队。命令 metadata 只能按运行时实际字段渲染。 |
| P2 / 条件 | **编辑器与用户上下文同步**：在未来存在可编辑文件视图或用户终端时，消费 `documentLifecycle`、`userEdits`、`terminalLifecycle`、`userShellCommand`；对 `userConfig`、`megaplan` 先做只读展示或契约探测。 | 必须先捕获并测试 vendor payload；只报告用户在宿主中的打开、聚焦、编辑或手动命令事件，不由 Desktop 代替 Devin 执行工具。 | 当前只有只读文件预览且没有用户终端，立即实现收益有限；vendor 方法没有公开稳定 schema，错误广告会造成数据错路由。 |
| P2 | **音频输入**：录音/文件音频作为 `ContentBlock::Audio`。 | `promptCapabilities.audio` 与当前模型 `supportsAudio` 同时为 true；补 shared wire type、媒体大小/格式校验和 renderer composer。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2713-L2717】【apps/desktop/src/shared/capabilities.ts:L138-L144】 | 当前实测 audio=false，且音频涉及麦克风权限、隐私和大 payload；必须保持 capability-gated。【apps/desktop/src/shared/capabilities.ts:L164-L167】 |
| P2 / 实验 | **MCP-over-ACP、providers、NES、plan/compaction 扩展、任意 ext methods**。 | 仅在 initialize 广告且 SDK schema/Devin 版本明确时启用；为未知扩展保留 raw payload 和超时/权限审计。【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2659-L2682】【apps/desktop/node_modules/@agentclientprotocol/sdk/schema/schema.json:L2282-L2284】 | ACP schema 明确标记多项 unstable；任意扩展可能改变权限或数据流，不能默认转发给本地执行器。 |
| 无需 Desktop 代码 | **Rules、Skills、MCP、Hooks、Plugins、Custom Subagents、Handoff**。 | 在 `.devin/`/用户配置中配置，由已安装 Devin CLI 执行；Desktop 仅刷新动态命令、显示事件和权限。【docs/devin-cli/extensibility/index.md:L53-L75】【docs/devin-cli/extensibility/plugins/overview.md:L49-L74】 | 保持“无第二 executor”；插件 closed beta、第三方 skill 可执行任意代码，需显示来源和信任提示。【docs/devin-cli/extensibility/skills/overview.md:L114-L120】【docs/devin-cli/extensibility/plugins/overview.md:L5-L17】 |

## 6. 实施不变量与验证

1. **只按 runtime capability 调用**：不能用静态常量替代 initialize/session/command response；当前 Host 已有 `requireCapability`，新增方法沿用同一门控。【apps/desktop/src/main/devin-acp-host.ts:L678-L691】【apps/desktop/src/shared/capabilities.ts:L89-L119】
2. **所有 Agent→Client 请求必须显式处理或 fail-closed**：SDK 已提供 typed handlers 和 extension parser 注册入口；不能把未知请求交给 Desktop 自己执行。【apps/desktop/node_modules/@agentclientprotocol/sdk/dist/acp.d.ts:L689-L738】【apps/desktop/src/main/acp-transport.ts:L191-L206】
3. **Desktop 不是第二执行器**：不要广告或实现 `clientCapabilities.fs`/`terminal`；文件、工具、终端、sandbox、MCP、Skills、Rules、Hooks、Plugins 和 Subagents 均由 Devin CLI 执行。Desktop 只处理用户交互、发送请求和展示结果；禁止读取或复制 Devin credentials。【docs/devin-cli/sandbox.md:L13-L27】【apps/desktop/src/main/devin-acp-host.ts:L637-L650】
4. **先 mock 后 live**：为每个 capability 分支增加 ACP mock 测试；认证 CLI 可运行 `DEVIN_LIVE_TEST=1 pnpm --dir apps/desktop smoke:devin`，并检查临时 session 是否删除、未知 vendor 字段是否仍被保留。【apps/desktop/scripts/live-cli-smoke.mjs:L33-L40】【apps/desktop/scripts/live-cli-smoke.mjs:L95-L118】【apps/desktop/src/main/devin-acp-host.test.ts:L95-L105】
